"""
1. pandas là gì
    - là thư viện mã nguồn mở, hỗ trợ đắc lực trong việc thao tác, xử lý và phân tích dữ liệu
    - đc sử dụng rộng rãi trong nghên cứu cx như phát triển các ứng dụng về khoa học dữ liệu

    - Ưu điểm:
        - tiện, nhanh, dễ
        - có thể xử lý nhiều tập dữ liệu khác nhau về định dạng khác nhau: chuỗi thời gian, bảng ko đồng nhất, ma trận
        - khả năng import từ nhiều nguồn khác nhau: CSV, DB/SQL
        - Có thể xử lý vô số các phép toán cho tập dữ liệu: Subsetting, Slicing, Filtering, Merging, groupBy, Re-order, reshape
        - Xử lý các dữ liệu mất mát (Loss) theo ý người dùng mong muốn: bỏ qua hoặc chuyển sang 0
        - xử lý, phần tích dữ liệu tốt như mô hình hoá và thống kê
        - Tích hợp tốt vs các thư viện khác của Python
2. các khái niệm: Series, 
    - Dataframe vs Series
    - đọc dữ liệu vào (import dataframe)

3. đọc ghi cơ bản với dataframe
    - Xem thông tin 
    - lấy data theo cột/nhiều cột
    - lấy data theo dòng/nhiều dòng: index, tail, head, iloc, loc
    - lấy theo điều kiện
4. sửa xoá
    - 
5. các thao tác thống kê sắp xếp
6. vẽ biểu đồ vs pandas

"""